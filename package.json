{
  "name": "transaction-notary",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "node ./tools/install.js",
    "build": "cd ./api && npm run package && cd ../ && node ./tools/copy-dist.js",
    "test": "cross-env TEST=test-all npm run test-docker-run",
    "test-ci": "cross-env TEST=test-all-ci npm run test-docker-run",
    "test-docker-run": "cd ./docker/compose/Test && docker-compose up --build --abort-on-container-exit",
    "package": "cd ./api && npm run package"
  },
  "devDependencies": {
    "cross-env": "^5.2.0",
    "ncp": "^2.0.0"
  }
}
